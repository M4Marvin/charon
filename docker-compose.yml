services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=/app/data/local.db
      - PORT=3000
    volumes:
      - v2app-data:/app/data

volumes:
  v2app-data:
